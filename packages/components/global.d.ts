declare module '@element-plus/icons-vue' {
  import type { DefineComponent } from 'vue';
  const Search: DefineComponent;
  const Refresh: DefineComponent;
  const RefreshLeft: DefineComponent;
  export { Search, Refresh, RefreshLeft };
  // 如果需要更多图标，在这里添加
}
